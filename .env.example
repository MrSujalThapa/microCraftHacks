# Copy to .env and fill in values for local development.
# Never commit .env — it is excluded from the npm package.

# Required when provider is openai (default)
OPENAI_API_KEY=

# Optional overrides (also configurable in .swarm/config.json)
SWARM_PROVIDER=openai
SWARM_MODEL=gpt-5-mini
